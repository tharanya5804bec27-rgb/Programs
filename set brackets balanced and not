def is_balanced(brackets_str):
    stack = []
    mapping = {")": "(", "}": "{", "]": "["}
   
    for char in brackets_str:
        if char in mapping.values():
            stack.append(char)ack
        elif char in mapping:
            if not stack or stack[-1] != mapping[char]:
                return False
            stack.pop()
    return len(stack) == 0
user_input = "{{}}{)([][]"
print(f"Is '{user_input}' balanced? {is_balanced(user_input)}")

