def recursive_length(s):
    if s == "":
        return 0
    return 1 + recursive_length(s[1:])
user_input = input("Enter a string to find its length: ")
result = recursive_length(user_input)
print(f"The length of the string is: {result}")
